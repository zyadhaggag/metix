{
    "name": "meetix-server",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "build": "npx esbuild index.ts --bundle --platform=node --outfile=dist/index.js --packages=external --format=esm",
        "start": "node dist/index.js"
    },
    "dependencies": {
        "express": "^5.1.0",
        "cors": "^2.8.5",
        "dotenv": "^17.3.1",
        "livekit-server-sdk": "^2.12.2",
        "@supabase/supabase-js": "^2.49.4"
    },
    "devDependencies": {
        "esbuild": "^0.25.0"
    }
}